SUBTREE_DIR=published_bfe/
rm -rf $SUBTREE_DIR
cp _config.bfe.yml _config.yml
jekyll build -d $SUBTREE_DIR
git add -A $SUBTREE_DIR
git commit -m "Auto-update: $(date)"
git subtree push --prefix=$SUBTREE_DIR bfepta master
