SUBTREE_DIR=published/
jekyll build -d $SUBTREE_DIR
git add -A $SUBTREE_DIR
git commit -m "Auto-update: $(date)"
# git push origin --delete gh-pages
git subtree push --prefix=$SUBTREE_DIR origin gh-pages
